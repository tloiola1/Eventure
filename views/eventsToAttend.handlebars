    <div class="container">
        <div class="row">
            <div class=" col-sm-12 profile">
                    <img src="../assets/images/cesar-couto-420986.jpg" alt="userImage">
                    <a id="colorGold" class="User" href="/profile">User Name</a>
                    <span id="colorGold"> | </span>
                    <a id="colorGold" class="logout" href="/logout">Log Out</a>
                </div>
            </div>
        <!-- </div> -->
        <!-- <div class="row"> -->
            <!--EVENT THE GUEST IS BEST MATCHED TOO-->
            <div class="text-center">
                <h1 class="col-sm-12 jumbotron eventsForYou">Events for You<hr></h1>
                <div class="mainSection"></div>
            </div>
        </div>
    </div>

    <script>
    console.log(localStorage.getItem("email"));
    var email = localStorage.getItem("email");
    var user = {
        email
    };
//        var survey;
//                $.ajax({
//                    url:"/findUser",
//                    method:"GET",
//                    data: user
//                }).then(function(answer){
//
//                    console.log( "This is survey: " + answer.survey);
//                    survey = answer.survey; // soccer

                $.ajax({
                    type:"GET",
                    url:"/ticketmaster/" + email,
                    async:true,
//                    data:user,
                    dataType: "json",
                    success: function(json) {
                        
                        console.log(json.length);
                      
                            for (var i = 0; i < 4; i++) {
                                var box =   `<div class="col-sm-3">
                                                <div class="thumbnail">
                                                    <span style="position: absolute; left: 10%; font-weight: bold">`+ json._embedded.events[i]._embedded.venues[0].name +`</span>
                                                    <img src="`+ json._embedded.events[i]._embedded.venues[0].images[0].url +`" alt="image">
                                                    <div class="caption">
                                                        <h3>`+ json._embedded.events[i].name +`</h3><hr>
                                                        <p>`+ json._embedded.events[i].dates.start.localDate +`</p>
                                                        <p>`+ json._embedded.events[i]._embedded.venues[0].boxOfficeInfo.phoneNumberDetail +`</p>
                                                        <p><a href="`+ json._embedded.events[i].url +`" class="btn btn-success " role="button" style="width: 100%" target="_blank">Get Your Ticket Now!</a></p>
                                                    </div>
                                                </div>
                                            </div>`;
                                $(".mainSection").append(box);
                            }
                        // Parse the response.
                        // Do other things.
                    },
                    error: function(xhr, status, err) {
                        // This time, we do not end up here!
                    }
                });
//                });

    </script>

<style>
    body{
        background-image: url("../assets/images/cesar-couto-420986.jpg");
        background-size: cover;
    }
    .profile img{
        width: 50px;
        height: 50px;
        border-radius: 50%;
    }
    #event{
        width: 100%;
    }

    footer{
        color: transparent !important;
    }

    #colorGold{
        color: gold;
    }
    .container{
        width: 90%;
    }
    .profile{
        text-align: right;
        position: relative;
        padding: 5px 30px 0 30px;
    }

</style>